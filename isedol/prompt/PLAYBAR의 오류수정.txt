첨부파일은 전체 코드이다. 현재 다음과 같은 오류가 있다. 오류를 해결하고 수정코드와 어디를 어떻게 수정해야 하는지 출력한다.

1. playbar에서 음악 재생은 되지만 playbar의 좌측에 존재하는 값들을 받아오지 못하고 있다. 이를 처음 fetchPlaybarPlaylist 함수가 동작할 때 같이 가져와서 출력되도록 한다.
2. playlist페이지에서 grid의 아이템을 클릭하여도 페이지가 이동되지 않는다.

 위 인덱스를 준수하여 페이지를 수정한다. 페이지는 spa방식으로 구현할 것이며 html, css, js를 한 파일에 저장한다. 요구사항을 제작하는데 필요한 라이브러리는 cdn 방식으로 가져온다. 디자인은 요구사항을 충족하는 동시에 PC와 모바일간의 UX를 고려하여 구성한다. 현재 모든 기능들과 디자인은 일체 수정하지 않고 요구사항에 요구한 조건에 해당하는 코드만 수정한다. 수정 혹은 추가가 필요한 코드를 출력하고 어디에 추가해야하는지 출력한다.


다시 수정하기 전 코드로 복원한 뒤 다음 요구사항에 맞춰 수정한다.

1. playlist에서 grid의 아이템을 클릭한 경우 플레이리스트를 갱신하는 것이 아닌 새로운 변수를 선언하여 플레이리스트의 idx값을 저장한다.
2. playlistAll 페이지에는 새로운 변수에 저장한 idx를 통해 play_list에 존재하는 노래들을 지금처럼 목록으로 출력한다.
3. 만약 사용자가 목록 중 한개를 클릭한 경우 그 때 playbar에 playlist를 갱신하고 클릭한 아이템의 songs의 idx를 시작으로 playbar의 기능이 동작한다.

 위 인덱스를 준수하여 페이지를 수정한다. 페이지는 spa방식으로 구현할 것이며 html, css, js를 한 파일에 저장한다. 요구사항을 제작하는데 필요한 라이브러리는 cdn 방식으로 가져온다. 디자인은 요구사항을 충족하는 동시에 PC와 모바일간의 UX를 고려하여 구성한다. 현재 모든 기능들과 디자인은 일체 수정하지 않고 요구사항에 요구한 조건에 해당하는 코드만 수정한다. 수정 혹은 추가가 필요한 코드를 출력하고 어디에 추가해야하는지 출력한다.

========================================================================

오류 한가지가 발생하였다. 다음 요구조건에 대한 수정코드와 어디를 수정하는지 출력한다.

1. playlistAll페이지에서 목록의 아이템을 클릭하면 playlist에서 클릭한 grid의 아이템이 가진 idx의 playlist가 playbar에 출력되어야 하는데 기본으로 설정한 playlist에서 index값에 따라 기존 음악이 계속 출력된다. 
2. 이것을 playlistAll페이지에서 목록의 아이템을 클릭하면 playlist에서 클릭한 grid의 아이템이 가진 idx의 playlist가 playbar에 출력되도록 수정한다.
3. 그 외 수정했던 페이지 이동관련 기능은 원하는 방향으로 동작하고 있으니 수정하지 않는다.

 위 인덱스를 준수하여 페이지를 수정한다. 페이지는 spa방식으로 구현할 것이며 html, css, js를 한 파일에 저장한다. 요구사항을 제작하는데 필요한 라이브러리는 cdn 방식으로 가져온다. 디자인은 요구사항을 충족하는 동시에 PC와 모바일간의 UX를 고려하여 구성한다. 현재 모든 기능들과 디자인은 일체 수정하지 않고 요구사항에 요구한 조건에 해당하는 코드만 수정한다. 수정 혹은 추가가 필요한 코드를 출력하고 어디에 추가해야하는지 출력한다.

========================================================================

랜덤재생 기능과 반복재생 기능 수정

 지금까지 수정한 내용을 기준으로 하여 랜덤재생, 반복재생 기능을 수정한다. 다음 요구사항에 맞춰 수정한다.

- 아래 기능들은 해당 버튼들이 클릭되었을 때 실행되는 함수들이다. 

1. 랜덤재생
1) playbar에서 재생하고 있는 currentPlaylist의 순서를 랜덤하게 섞는다

2. 반복재생
1) currentPlaylist에서 현재 재생되고 있는 노래가 반복재생된다.
2) 재생되는 노래가 끝난다고 해도 동일한 노래가 반복재생한다.

 위 인덱스를 준수하여 페이지를 수정한다. 페이지는 spa방식으로 구현할 것이며 html, css, js를 한 파일에 저장한다. 요구사항을 제작하는데 필요한 라이브러리는 cdn 방식으로 가져온다. 디자인은 요구사항을 충족하는 동시에 PC와 모바일간의 UX를 고려하여 구성한다. 현재 모든 기능들과 디자인은 일체 수정하지 않고 요구사항에 요구한 조건에 해당하는 코드만 수정한다. 수정 혹은 추가가 필요한 코드를 출력하고 어디에 추가해야하는지 출력한다.

셔플은 동작하지 않는다.